# ogcproxy
Water Quality Portal (WQP) OGC Proxy

## Local Configuration
This application is configured to be run in a Tomcat container. The development configuration (context.xml and ogcproxy.properties) can be copied to your local Tomcat. Required configuration is:

```
(nothing yet)
```
